version: '3'
services:
  app:
    build:
      context: .
      dockerfile: ./Dockerfile
    command: sh -c "pnpm install && pnpm run dev -- --host 0.0.0.0"
    ports:
      - '5173:5173'
    volumes:
      - .:/var/www/html
